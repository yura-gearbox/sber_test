# Тестовое задание

## Окружение и запуск тестов

Для работы тестов нужно установить зависимости:

`pip install -r requirements.txt`

Запуск всех тестов производится командой:

`pytest .`

Каждое задание помечено маркером:

`api` - тесты API httpbin.org из задания 1

`ui` - тесты UI страницы https://online.sberbank.ru/CSAFront/index.do из задания 2

`str_to_int` - тесты функции `str_to_int` из задания 3

Для запуска конкретных тестов надо указать маркер:

`pytest -m api`

### Файлы окружения

`pytest.ini` - настройки pytest и маркеры

`requirements.txt` - список зависимостей

`README.md` - описание, которое вы читаете

### Тесты httpbin

Сервис `httpbin.org` доступен не всегда. В этом случае для проверки работы тестов API можно запустить на хосте локальную версию `httpbin`. Для этого:

Установите зависимости:

`pip install gunicorn httpbin`

Запустите сервис:

`gunicorn httpbin`

В файле `data.py` есть строки с адресом сервиса:

`BASE_URL = 'http://httpbin.org'`  
`# BASE_URL = 'http://127.0.0.1:8000'`

Закомментируйте первую строку и раскомментируйте вторую. Теперь API тесты будут работать с локальной версией сервиса.

## 1\. Задания на работу с api запросами

Напишите автотесты, которые будут проверять статусы и ответы запросов к следующим сервисам.

1.  http://httpbin.org/get
2.  http://httpbin.org/post
3.  http://httpbin.org/status/{codes} метод GET
4.  http://httpbin.org/delay/{delay} метод GET
    - Удостовериться, что запрос выполняется с задержкой, переданной в запросе. Тест должен быть параметризованный, параметры должны браться из json файла в корне проекта
5.  http://httpbin.org/response-headers?freeform=
    - Тест должен быть параметризованный, параметры должны браться из json файла в корне проекта

### Файлы задания

`test_httpbin.py` - тесты задания

`api_client.py` - класс `ApiClient` для доступа к API и функции проверки полей response

`models.py` - модели `pydantic` для проверки json-схем

`data.py` - статичные тестовые данные

`parameters.json` - тестовые данные для тестов 4 и 5

## 2\. Задание на UI тесты

1.  Перейти на страницу https://online.sberbank.ru/CSAFront/index.do
2.  Страница должна загрузиться в течение 10 секунд
3.  Ввести логин Avtotest
4.  Ввести pswd 123456
5.  Нажать кнопку Продолжить
6.  Проверить ожидаемый результат: появится сообщение об ошибке

### Файлы задания

`test_ui.py` - тесты задания

`base_page.py` - класс `BasePage`, в котором определены примитивы для работы со страницей

`ui_data.py` - тестовые данные и локаторы

## 3\. Задача на программирование

Реализуйте функцию `def str_to_int(str)`:

- Функция принимает строку и возвращает тип int
- In: "275295"
- Out: 275295
- Требования: Прямые преобразования (cast) из одного типа данных в другой, а также использование массивов запрещены.
- Подсказка: Используйте ASCII table

### Файлы задания

`test_str_to_int.py` - содержит требуемую по заданию функцию `str_to_int(str)`, а также тесты для проверки ее работы
